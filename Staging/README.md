# Staging

Files waiting for an Xcode target that only the Xcode wizard can create. Once the target exists,
these replace the wizard's template files and this folder is deleted. Nothing here is compiled.

- `ScreenTimeNextWidgets/` → Widget Extension target `ScreenTimeNextWidgets` (Live Activity, D-014).
